import { createTheme } from '@mui/material/styles';

const CorpTheme = createTheme({
    palette: {
        primary: {
          main: '#035B71',
        },
        secondary: {
          main: '#00A2B9',
        },
      },
});

export default CorpTheme;